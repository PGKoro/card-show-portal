name: CI

on:
  pull_request:
    branches: [main, staging]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI placeholder — real frontend/backend jobs added once apps are scaffolded"
